spring:
  datasource:
    url: jdbc:h2:mem:identity_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  sql:
    init:
      mode: never

app:
  jwt:
    secret: test-secret-key-must-be-at-least-256-bits-long-for-hs256-algo!!
    expiration-ms: 3600000
